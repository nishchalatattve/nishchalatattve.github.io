[project]
name = "notes-and-snippets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "zensical>=0.0.28",
]

[dependency-groups]
dev = [
    "zensical>=0.0.23",
]
