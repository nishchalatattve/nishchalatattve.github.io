[project]
name = "haoze-notes"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "zensical>=0.0.34",
]

[dependency-groups]
dev = [
    "zensical>=0.0.23",
]
